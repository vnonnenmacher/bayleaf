# Required
DJANGO_SECRET_KEY=change-me-very-random

# RSA keys (PEM). Use \n for newlines if you keep them inline,
# or see note below about mounting from files.
BAYLEAF_RSA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
BAYLEAF_RSA_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# Strongly recommended
ALLOWED_HOSTS=api.yourdomain.com,app.yourdomain.com

# MinIO / documents
# If MINIO_ACCESS_KEY / MINIO_SECRET_KEY are omitted,
# Bayleaf will fall back to MINIO_ROOT_USER / MINIO_ROOT_PASSWORD.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change-me
MINIO_ENDPOINT=minio:9000
MINIO_PUBLIC_ENDPOINT=localhost:9000
# Optional explicit API credentials (defaults to root creds above)
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_USE_SSL=false
MINIO_PUBLIC_USE_SSL=false
MINIO_REGION=
BAYLEAF_DOCS_BUCKET=bayleaf-documents
MINIO_PRESIGN_EXPIRES=900
